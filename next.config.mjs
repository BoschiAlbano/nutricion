/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["via.placeholder.com"], // Añade los dominios de las imágenes que deseas permitir
    },
};

export default nextConfig;
